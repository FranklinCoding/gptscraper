# Polling interval in seconds for stock news sources
POLL_SECONDS=15

# Historical backfill window at startup (days)
BACKFILL_DAYS=7

# Optional path to custom ticker symbol list (one symbol per line)
# TICKER_FILE=/app/data/tickers.txt
